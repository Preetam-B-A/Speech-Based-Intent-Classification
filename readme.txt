Install all the libraries in requirements.txt
Install ffmpeg and set it's path in the environment variable
Store the audio files in directory called 'audio' in the sae directory.
Run the code on Command Prompt by typing 'python app.py'